{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "termino.schema.json",
  "title": "Término del glosario",
  "type": "object",
  "required": ["id", "termino", "definicion", "url"],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
    "termino": { "type": "string" },
    "definicion": {
      "type": "string",
      "minLength": 80,
      "maxLength": 700,
      "description": "Definición de consenso en palabras sencillas, SIN sobresimplificar: si el término está en disputa, la definición lo dice y presenta ambas lecturas."
    },
    "url": { "type": "string", "pattern": "^https://", "description": "Enlace de referencia (normalmente Wikipedia en español)" }
  },
  "additionalProperties": false
}
